.. _Core:

****************
Ядро
****************

При создании объекта ядра в него передаются параметры для работы всех его частей.
После создания экземпляра его нужно проинициализировать чтобы создать все необходимые вспомогательные интсрументы.


.. autoclass:: core.Core
    :members: __init__, init

